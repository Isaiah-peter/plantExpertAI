
import ScanScreen from '@/src/screens/ScanScreen';
// You can build a ScanScreen component in src/screens and import it here
export default function ScanPage() {
  
  // To move to results statically for now:
  // router.push('/result'); 

  return <ScanScreen />; // This will render the ScanScreen component when this page is accessed
}